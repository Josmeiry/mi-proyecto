import type { Config } from "tailwindcss";

const config: Config = {
  
  theme: {
  extend: {
    colors: {
      surface: "#000000",
      background: "#ffffff",
      primary: "#c8610c",
      secondary: "#467879",
      border: "#2a3733",
      
      
    },
  },
},
darkMode: "class",
  
};

export default config;
